# Variables de entorno para SW4 - Perfumes Inventory System
# Copia este archivo a .env y llena los valores

# Google Sheets Configuration
# Sheet ID del proveedor (Source of Truth - READ ONLY)
GOOGLE_SHEET_PROVEEDOR_ID=your_google_sheet_id  
# Sheet ID interno (Productos con precios calculados + stock)
GOOGLE_SHEET_ID=Google_sheet_id

# Service Account JSON (copia todo el contenido del archivo .json)
GOOGLE_SERVICE_ACCOUNT_JSON=your_service_account

# MercadoPago Configuration (Venezuela)
MP_ACCESS_TOKEN=tu_mercadopago_access_token_aqui

# IMPORTANTE: Nunca subas el archivo .env a git
# Ya está incluido en .gitignore
